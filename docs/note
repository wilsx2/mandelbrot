9:  Make color & orbit (-> escape) unified
    DoubleDouble (device & host)
    Free FloatExp from Boost backend (w/ benchmark), unify
10: Unified BLA (fix bugs introduced earlier)
11: Integrate with Bench & CLI
12: Mirror improvements from device code in host code, Optimize

July
- GPU
    - Features 
        X Coloring
        X Direct Escape
        X Perturbed Escape
        - Generalize to many types
            - float
            - Quad
            - FloatExp
        - BLA
    - Benchmark
    - CLI
- Distribute / Parallel
    - Features 
        - Shared filesystem (Amazon EFS/S3, MinIO)
        - MPI
    - Profile & Optimize
- Time-target (as opposed to zoom-target) Video Mode
    - Calculate reference in parallel with frame rendering
- Local Machines
- Cloud (Amazon EC2, g4dn?)

- Type traits for float -> complex conversion
- Move most things to headers to allow for inlining optimizations
- Float in the reference set
- Replace long double with a DoubleDouble type for both Windows and GPU compatability
- Make as many functions/classes as possible compatible with CUDA and C++ code (DRY)
- Switch to unsigned int for iterations, especially in GPU code
- ? Cache BLAs coefficients (if BLA calculation becomes a bottleneck)
  ? Fix glitch (if existence becomes evident)
- ? Use periods (if above)
- ? Adapt threshholds to per-pixel dc rather than zoom depth (if inconsistent behavior between resolutions)
- ? Tiling (if 4k frames take long enough to benefit from distributed parallelization)
- ? Analyze for zoom points (if we need a deeper zoom)
